# Lab 02 Problem

## [Problem 01 : พื้นที่และเส้นรอบวง](./PB_01.py)

### โจทย์ : พื้นที่และเส้นรอบวง

จงเขียนโปรแกรมเพื่อคำนวณหาพื้นที่วงกลมและเส้นรอบวงของวงกลมที่มีรัศมี `r`

กำหนดให้ข้อมูลเข้าเป็นเลขจำนวนเต็มเท่านั้น แสดงพื้นที่และเส้นรอบวงเป็นเลขจำนวนจริงทศนิยม 2 ตำแหน่ง

แนะนำ ใช้ตัวแปร `math.pi` ใน module math แทนค่า Pi

### ข้อมูลเข้า
บรรทัดเดียว แสดงข้อความ `"Enter a radius: "` และรอรับจำนวนเต็ม r ซึ่งไม่เป็นลบ แทนรัศมีของวงกลม

### ข้อมูลออก
บรรทัดแรก แสดงข้อความ `"Area of a circle with radius "` ตามด้วยค่า `r` ตามด้วย `" is "` ตามด้วยพื้นที่ของวงกลมรัศมี r

บรรทัดที่สอง แสดงข้อความ `"Circumference of a circle with radius "` ตามด้วยค่า `r` ตามด้วย `" is "` ตามด้วยเส้นรอบวงของวงกลมรัศมี r

ตัวอย่าง Input/Output

```
Enter a radius: 3
Area of a circle with radius 3 is 28.27
Circumference of a circle with radius 3 is 18.85
```

### hint : หาวิธีใช้ method (ได้จาก silde leture) แล้วอย่าลืมทศนิยม 2 ตำแหน่งด้วยละ โดยการใช้ format ประเภทไหนก็ได้ตามต้องการเลย

## [Problem 02 : สระว่ายน้ำ](./PB_02.py)

#### โจทย์ : สระว่ายน้ำ

กำหนดให้สระว่ายน้ำ (pool) เป็นรูปสี่เหลี่ยมผืนผ้า โดยเราต้องการเติมน้ำลงไปในสระให้เต็ม จงเขียนโปรแกรมเพื่อรับความกว้าง ความยาว และความลึกของสระในหน่วยเมตร เพื่อคำนวณว่า ต้องใช้เวลากี่นาทีในการเติมน้ำลงในสระให้เต็ม เมื่อกำหนดให้ใช้เวลา 15 วินาทีในการเติมน้ำ 1 ลูกบาศก์เมตร

#### สมมติฐานของข้อมูลนำเข้า `ความกว้าง` `ความยาว` และ `ความลึก` เป็นจำนวนจริง

#### แสดงคำตอบด้วยทศนิยมสองตำแหน่ง

ตัวอย่าง Input/Output
```
Enter width: 18.5
Enter length: 38.75
Enter depth: 1.35
Time to fill a pool is 241.95 minutes.
```

### hint : อย่าลืมทศนิยม 2 ตำแหน่งด้วยละ โดยการใช้ format ประเภทไหนก็ได้ตามต้องการเลย

## [Problem 03 : พิมพ์ตามสั่ง](./PB_03.py) 

#### โจทย์ : พิมพ์ตามสั่ง 
เขียนโปรแกรมเพื่อรับจำนวนจริงสองจำนวน คือ `inc` และ `ex` แล้วพิมพ์ข้อความตามรูปแบบต่อไปนี้

### ตัวอย่าง Output

(สมมติรับ input เป็น 145.67 และ -20.25 ตามลำดับ)
```
123456789012345678901234567890
Total Income  +145.67 bahts
Expense        -20.25 bahts
Profit       00125.42 bahts
```
ตัวอย่าง Output
(สมมติรับ input เป็น 2000.3 และ -2.25 ตามลำดับ)
```
123456789012345678901234567890
Total Income +2000.30 bahts
Expense         -2.25 bahts
Profit       01998.05 bahts
```
(ผลลัพธ์ต้องการให้จุดทศนิยมตรงกัน)
### hint : แนะนำให้ใช้ [F-String](https://www.w3schools.com/python/python_string_formatting.asp) เพื่อการอ่านที่ง่าย(คล้าย string ปกติ) ดูตำแหน่ง `123456789012345678901234567890` เป็นหลัก 
### keyword : aligns, sign, point 

ปล. ข้อนี้ตรงเน้นหาความรู้จาก doc สู้ๆในลองผิดลองถูกนะ ⭐✌️

## [Problem 04 : ผลรวมเศษส่วน](./PB_04.py)

#### โจทย์ : ผลรวมเศษส่วน
จงเขียนโปรแกรมเพื่อรับค่าเศษ `(numerator)` และค่าส่วน `(denominator)` ของเศษส่วน `(fraction)` สองจำนวน แล้วคำนวณหาผลรวมของเศษส่วนทั้งสองจำนวน

สมมติให้เศษส่วนแรกอยู่ในรูป `a/b` และเศษส่วนที่สองอยู่ในรูป `c/d` ในที่นี้ให้แสดงผลลัพธ์ `p/q` ซึ่งเป็นผลรวมที่ได้ในรูปเศษส่วนเช่นกัน และไม่ต้องทำให้เป็นเศษส่วนอย่างต่ำ

ตัวอย่าง Input/Output (อักษรที่เส้นใต้เป็นสีแดงคือ input)##
```
First fraction:
>>Enter a numerator a: 3
>>Enter a denominator b: 8
Second fraction:
>>Enter a numerator c: 9
>>Enter a denominator d: 12
Summation of the two fractions is 108 / 96
```

### hint : ก็ถ้าพี่เรียนประถมมาคงคูณเศษส่วนเป็นอะนะ

## [Problem 05 : เติมน้ำมัน](./PB_05.py)

#### โจทย์ : เติมน้ำมัน
แก้วกับขวัญพักอยู่ที่คอนโดเดียวกันและเพิ่งซื้อรถใหม่มาคนละคันเป็นรถยี่ห้อเดียวกันรุ่นเดียวกันเหมือนกันทุกอย่าง รถรุ่นนี้เป็นอีโคคาร์จึงสามารถเดินทางได้ถึง `15 กิโลเมตรต่อน้ำมัน 1 ลิตร`

แต่แก้วเป็นคนขี้กังวลจึงเลือกที่จะเติมน้ำมันเมื่อปริมาณน้ำมันรถเหลือ `50% ของความจุถัง`ในขณะที่ขวัญเป็นคนที่ชอบความท้าทายจึงเติมน้ำมันเมื่อปริมาณเหลือ `10% ของความจุถัง` เมื่อน้ำมันลดถึงจุดที่กำหนดทั้งแก้วและขวัญจะเติมน้ำมันทันทีแม้ว่าจะถึงจุดหมายแล้ว

เพื่อฉลองรถใหม่แก้วกับขวัญใช้รถของตนเองออกเดินทางไปยังจุดหมายที่ใช้ระยะทางเท่ากัน โดยทั้งสองคนจะเติมน้ำมันครั้งแรกที่ปั๊มที่อยู่ติดกับคอนโดจนเต็มถังไม่ว่ารถจะมีน้ำมันเหลือเท่าไหร่ แต่การเติมน้ำมันครั้งต่อๆ ไปจะเป็นไปตามข้อกำหนดที่กล่าวไว้ ทั้งสองคนมีน้ำมันสำรองที่พร้อมจะเติมเสมอ

#### ให้นิสิตเขียนโปรแกรม รับข้อมูลเลขจำนวนเต็มตัวแรกคือ `ระยะทางที่แก้วกับขวัญเดินทางเป็นกิโลเมตร` และในบรรทัดต่อไป `เป็นความจุของถังน้ำมันเป็นลิตร` ซึ่งเป็นเลขจำนวนเต็มเช่นกัน จากนั้นให้พิมพ์`จำนวนครั้งที่`แก้วและขวัญ`ต้องเติมน้ำมัน`ในการเดินทางไปถึงจุดหมาย (รวมการเติมครั้งแรกด้วย)โดยพิมพ์ของแก้วก่อนในบรรทัดแรกและตามด้วยของขวัญในบรรทัดที่สอง จำนวนครั้งที่เติมน้ำมันต้องเป็นเลขจำนวนเต็ม

#### ตัวอย่างข้อมูลเข้า/ข้อมูลออก
| ข้อมูลเข้า   | ข้อมูลออก |
|-----------|---------|
| 400<br>10 | 6<br>3  |
| 540<br>12 | 7<br>4  |

### hint : ข้อนี้ยาว อ่านข้อความหน้าและหลัง highlight ด้วย ทำการ input กับ output และส่วนสำคัญไว้ให้แล้ว


## [Problem 06 : พิมพ์ดาวบรรทัดเดียว](./PB_06.py)

#### โจทย์ : พิมพ์ดาวบรรทัดเดียว
ให้นิสิตเขียนโปรแกรมรับค่า n ซึ่งเป็นเลขจำนวนเต็มบวก และรับอักขระ 2 ตัว บรรทัดละตัว แล้วพิมพ์อักขระความยาว n ตัวในบรรทัดเดียวกัน โดยให้พิมพ์อักขระ 2 ตัว สลับกันไป `โดยไม่ใช้ if-else หรือ loop statement`
| ข้อมูลเข้า   | ข้อมูลออก |
|-----------|---------|
| 6<br>$<br># | `$#$#$#` |
| 5<br>$<br># | `#$#$#`  |
### hint : ข้อนี้ลงเขียนอนุกรณ์ง่ายดู เล่นกับเศษหาร หรือ พี่จะลองหาวิธีใช้ [String Slicing](https://www.geeksforgeeks.org/string-slicing-in-python/) ก็ได้นะ

## [Problem 07 : ฟังก์ชันทางคณิตศาสตร์](./PB_06.py)

#### โจทย์ : ฟังก์ชันทางคณิตศาสตร์
จงเปลี่ยนนิพจน์ทางคณิตศาสตร์ข้างล่างนี้เป็นนิพจน์ทางคณิตศาสตร์ในภาษา Python ที่ถูกต้อง

$$ a1 = x^y + z $$
$$ a2 = cos(2\pi) + \log_e (x) $$
$$ a3 = |x| + |y| $$
$$ a4 = \sqrt{x^2 +y^2+z^2} $$
$$ a5 = \sin^2(x) + \cos^2(x) $$
$$ a6 = \sqrt[5]{x+y} $$
$$ a7 = e^{x \ln y} $$

หมายเหตุ: สำหรับค่าพายให้ใช้ค่าคงที่ `math.pi` และค่า e ให้ใช้ค่าคงที่ `math.e`

ดูรายละเอียดของตัวแปรและฟังก์ชันในโมดูล math ได้ที่ https://docs.python.org/3.6/library/math.html

#### วัตถุประสงค์ของโจทย์ข้อนี้ ต้องการให้นิสิตอ่าน documentation ⭐

ตัวอย่าง Input/Output
```
Enter x: 5.0
Enter y: 5.0
Enter z: 5.0
a1 = 3130.00
a2 = 2.61
a3 = 10.00
a4 = 8.66
a5 = 1.00
a6 = 1.58
a7 = 3125.00
```

### hint : ตามตัวหน้าเลย มาฝึกอ่าน doc กับ seach google กัน และก็เรียนวิธีการใช้ method ที่คล้ายๆ PB 1 อะ 

#### Extra : แนะนำให้เรียนวิธีกำหนด import เป็นตัวแปรก็ดีนะ จะได้ใช้เขียน code เร็วขึ้นและในอนาคตได้ใช้แน่นอน 

## บ่นไปเรื่อย

#### ก็เข้า week 2 แล้วก็ขอขายเพลงบ้างละกัน
https://www.youtube.com/watch?v=2DGf-KlKXGA

ส่วนตัวไม่ใช่ main haruka แต่ Yunosuke แต่งเพลงดีจริงรวมกับ haruno mv โคตรสวย แล้ว ver vocaloid ดีมากกกก ถึงปกติจะไม่ฟังอะนะ แนะนำให้ไปตามกันนนนนนน

#### เข้าจริงจังละ

ต้องยอมรับเลย lab week นี้เร็วและงงมาก ถ้าคุณไม่เคยเขียนอะไรที่เกี่ยวกับ format มาดีที่ python มันมีหลายวิธี ส่วนตัวเลยเรียนมากก่อนเลยไม่มีปัญหาแต่ flashback ไปแบบตอนนี้ %s %d ครั้งแรก ร้องเป็นหมาเหมือนกันเพราะฉะนั้น ผมหวังว่า readme และ solution จะเป็นตัวช่วยไม่น้อยก็มาสำหรับทุกคน ผมพยายามเขียนอธิบายใน hint ไม่ให้ spoil เกินไป อาจจะไม่ได้อธิบายในนี้มาก แต่ใน lab หน้าถ้ามีความยากขึ้นจะพยายาม highlight ส่วนสำคัญ อธิบาย input ouput ให้ง่ายขึ้นเพื่อเพื่อนบ้างคงยังงงกันสิ่งที่โจทย์ต้องการ ขอบคุณที่อ่าน ลงชื่อ MMJ lover